<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-feature
        android:name="android.hardware.sensor.accelerometer"
        android:required="true" />
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:requestLegacyExternalStorage="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.XPNSR"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">
        <!-- Messagging color -->
        <!-- [START fcm_default_icon] -->
        <!-- Set custom default icon. This is used when no icon is set for incoming notification messages.
             See README(https://goo.gl/l4GJaQ) for more. -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@drawable/baseline_computer_24" />
        <!-- Set color used with incoming notification messages. This is used when no color is set for the incoming
             notification message. See README(https://goo.gl/6BKBk7) for more. -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_color"
            android:resource="@color/common_google_signin_btn_text_light" />
        <!-- [END fcm_default_icon] -->
        <!-- [START fcm_default_channel] -->
        <activity
            android:name=".ui.activities.TransactionsActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.activities.TransactionViewActivity" />
        <activity android:name=".ui.activities.TransactionDetailsActivity" />
        <activity android:name=".ui.activities.TransactionActivity" />
        <activity android:name=".ui.activities.ReportActivity" />
        <activity android:name=".ui.activities.MapViewActivity" />
        <activity android:name=".ui.activities.ProfilePhotoActivityOld" />
        <activity android:name=".ui.activities.ProfilePhotoActivity" />
        <activity android:name=".ui.activities.VideoPlayActivity" />
        <activity android:name=".ui.activities.AnimationActivity" />
        <activity android:name=".ui.activities.ApiCallActivity" />
        <activity android:name=".ui.activities.TransactionListActivity" />
        <activity android:name=".ui.activities.TransactionsActivity2" />

        <service
            android:name=".utils.MyFirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
    </application>

</manifest>